{% extends 'base.html.twig' %}

{% block title %}Tiempo ofrecido por AEMET{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.3.0/semantic.min.css"/>
    <style type="text/css">
        body {
            background-color: #DADADA;
        }
        body > .grid {
            height: 100%;
        }
        .image {
            margin-top: -100px;
        }
        .column {
            max-width: 450px;
        }
        main-container{
            margin: 50px auto;
            width: 600px;
        }
        table{
            background-color: white;
            text-align: left;
            border-collapse: collapse;
            width: 100%;
        }

        th, td{
            padding: 20px;
        }

        thead{
            background-color: #246355;
            border-bottom: solid 5px #0F362D;
            color: white;
        }

        tr:nth-child(even){
            background-color: #ddd;
        }

        tr:hover{
            background-color: #369681;
            color: white
        }
    </style>
{% endblock %}
{% block body %}

    {{ parent() }}

{#
    <div class="ui equal width internally celled grid">
        <div class="center aligned row">
            <div class="column">
                <ul>
                    {% for item in viento_ahora %}
                        <li>{{ item['periodo'] }} {{ item['velocidad'] }}</li>
                    {% endfor %}
                </ul>
            </div>
            <div class="column">
                <ul>
                    {% for item in estado_cielo_ahora %}
                        <li>{{ item['periodo'] }} {{ item['descripcion'] }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
    <div class="ui equal width internally celled grid">
        <div class="center aligned row">
            <div class="column">
                <ul>
                    {% for item in viento_hoy %}
                        <li>{{ item['periodo'] }} {{ item['velocidad'] }}</li>
                    {% endfor %}
                </ul>
            </div>
            <div class="column">
                <ul>
                    {% for item in estado_cielo_ahora %}
                        <li>{{ item['periodo'] }} {{ item['descripcion'] }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
#}


    <div class="ui equal width internally celled grid">
        <div></div>
        <div class="center aligned row">
            <div class="column">
                <ul>Ahora</ul>
                <ul>cielo: {{ estado_cielo_ahora }}</ul>
                <ul>viento: {{ viento_ahora }}</ul>
                <ul>precipitacion: {{ precipitacion_ahora }}</ul>
                <ul>cota de nieve: {{ cota_de_nieve_ahora }}</ul>
                <ul>temperatura: {{ temperatura_ahora }}</ul>
            </div>
            <div class="column">
                <ul>Hoy</ul>
                <ul>cielo: {{ estado_cielo_hoy }}</ul>
                <ul>viento medio: {{ viento_hoy }}</ul>
                <ul>precipitacion: {{ precipitacion_hoy }}</ul>
                <ul>cota de nieve: {{ cota_de_nieve_hoy }}</ul>
                <ul>temperatura maxima: {{ temperatura_maxima_hoy }}</ul>
                <ul>temperatura minima: {{ temperatura_minima_hoy }}</ul>
            </div>
            <div class="column">
                <ul>Mañana</ul>
                <ul>cielo: {{ estado_cielo_manana }}</ul>
                <ul>viento medio: {{ viento_manana }}</ul>
                <ul>precipitacion: {{ precipitacion_manana }}</ul>
                <ul>cota de nieve: {{ cota_de_nieve_manana }}</ul>
                <ul>temperatura maxima: {{ temperatura_maxima_manana }}</ul>
                <ul>temperatura minima: {{ temperatura_minima_manana }}</ul>
            </div>
        </div>
    </div>

    {#<div id="main-container">
        <table>
            <thead>
            <tr>
                <th>Numero de viaje</th><th>Maletas</th><th>Compañia</th>
            </tr>
            </thead>

            <tr>
                <td>Agua</td><td>Tusmuertos</td>
            </tr>
        </table>
    </div>#}
{% endblock %}